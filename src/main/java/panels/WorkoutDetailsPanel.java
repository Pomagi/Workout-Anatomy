package panels;

import javax.swing.*;
import java.awt.*;

public class WorkoutDetailsPanel extends JPanel {

    public WorkoutDetailsPanel() {
        setLayout(new BorderLayout());
        setPreferredSize(new Dimension(1000, 700));
        setBackground(Color.RED);
    }
}
